apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Point to base configuration
resources:
- base

# Namespace (uncomment and set your namespace)
# namespace: eva

# Image customization (useful for different environments)
# images:
# - name: eva-seqcol
#   newName: your-registry/eva-seqcol
#   newTag: v1.0.0
